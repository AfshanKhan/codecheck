# codecheck

A local, tiered code review tool — deterministic linters plus an optional
local or cloud LLM pass, for reviewing a diff/PR or auditing a whole repo.

🚧 Repository bootstrap commit. The actual tool is on its way in via PR —
this commit just establishes the repo (license, gitignore). Check the PRs
for the real content.
